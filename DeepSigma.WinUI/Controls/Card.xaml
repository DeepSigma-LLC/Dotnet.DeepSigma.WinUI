<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DeepSigma.WinUI.Controls.Card"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeepSigma.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="8"
        Padding="{x:Bind CardPadding, Mode=OneWay}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock
                Text="{x:Bind Title, Mode=OneWay}"
                Visibility="{x:Bind TitleToVisibility(Title), Mode=OneWay}"
                Style="{ThemeResource BodyStrongTextBlockStyle}"
                Margin="0,0,0,8"/>

            <ContentPresenter
                Grid.Row="1"
                Content="{x:Bind CardContent, Mode=OneWay}"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>
